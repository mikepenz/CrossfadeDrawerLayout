<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.mikepenz.crossfadedrawerlayout.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name="com.mikepenz.crossfadedrawerlayout.app.CustomApplication"
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <!-- android:supportsRtl="true" -->
        <activity
            android:name="com.mikepenz.crossfadedrawerlayout.app.SimpleActivity"
            android:label="@string/app_name"
            android:theme="@style/MaterialDrawerTheme.TranslucentStatus">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
